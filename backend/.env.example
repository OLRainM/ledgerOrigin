# 后端环境变量示例。复制为 .env 或在系统中设置后再运行。
# 注意：真实密钥不要提交到仓库。

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=ledger_origin

# JWT 签名密钥，请改为足够复杂的随机字符串
JWT_SECRET=please_change_to_a_long_random_secret
