<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".App"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.LedgerOrigin"
        tools:targetApi="31">

        <activity
            android:name=".ui.login.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.LedgerOrigin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.login.LoginActivity" android:exported="false" />
        <activity android:name=".ui.login.RegisterActivity" android:exported="false" />
        <activity android:name=".ui.main.MainActivity" android:exported="false" />
        <activity android:name=".ui.record.AddRecordActivity" android:exported="false" />
        <activity android:name=".ui.category.CategoryManageActivity" android:exported="false" />
        <activity android:name=".ui.account.AccountManageActivity" android:exported="false" />

        <service
            android:name=".service.SyncService"
            android:exported="false" />

    </application>

</manifest>
